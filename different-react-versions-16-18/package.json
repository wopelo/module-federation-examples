{
  "name": "different-react-versions-16-18",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "npx lerna run --scope @different-react-versions-16-18/* --parallel start",
    "build": "npx lerna run --scope @different-react-versions-16-18/* build",
    "serve": "npx lerna run --scope @different-react-versions-16-18/* --parallel serve",
    "clean": "npx lerna run --scope @different-react-versions-16-18/* --parallel clean"
  }
}
